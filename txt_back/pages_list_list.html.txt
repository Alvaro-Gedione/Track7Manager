<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/png" href="../../public/img/meet360.png">
    <title>Track7 – Log de Mensagens</title>
    <link rel="stylesheet" href="../global.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <script src="../../components/topnav.js"
        onerror="console.warn('topnav.js não encontrado. Verifique se o arquivo existe na pasta raiz.')"></script>
    <style>
        /* Animações e estilos específicos para list - seguindo padrão do live-tracking */
        .list-container {
            padding: 0px 0px 30px 0px;
            max-width: 1600px;
            margin: 0 auto;
            animation: fadeInScale 0.5s ease-out;
        }

        @keyframes fadeInScale {
            from {
                opacity: 0;
                transform: scale(0.95);
            }

            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        .layout {
            display: flex;
            height: calc(100vh - 180px);
            gap: 15px;
            position: relative;
            align-items: stretch;
            margin-left: 30px;
        }

        /* Sidebar no formato do live-tracking */
        .filters-panel {
            width: 350px;
            background: var(--card-bg-solid);
            border: 1px solid var(--border-color);
            border-radius: 24px;
            display: flex;
            flex-direction: column;
            overflow: hidden;
            height: 100%;
            transition: all 0.3s ease;
        }

        /* Adicione esta nova classe para a área rolável */
        .filters-content {
            flex: 1;
            overflow-y: auto;
            /* Permite rolagem vertical */
            overflow-x: hidden;
            padding-bottom: 16px;
        }

        /* Personalizar a barra de rolagem da sidebar */
        .filters-content::-webkit-scrollbar {
            width: 4px;
        }

        .filters-content::-webkit-scrollbar-track {
            background: var(--border-color);
            border-radius: 4px;
        }

        .filters-content::-webkit-scrollbar-thumb {
            background: var(--brand-orange);
            border-radius: 4px;
        }

        .filters-content::-webkit-scrollbar-thumb:hover {
            background: #ff8c52;
        }

        .filters-panel:hover {
            border-color: rgba(242, 101, 34, 0.3);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        /* Search box igual ao live-tracking */
        .search-box {
            padding: 15px 20px;
            border-bottom: 1px solid var(--border-color);
            animation: fadeIn 0.7s ease-out;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .search-box i {
            color: var(--text-secondary);
            font-size: 14px;
        }

        .search-box input {
            flex: 1;
            padding: 8px 12px;
            border: 1px solid var(--border-color);
            border-radius: 20px;
            background: var(--input-bg);
            color: var(--text-primary);
            font-size: 13px;
            transition: all 0.3s ease;
        }

        .search-box input:focus {
            border-color: var(--brand-orange);
            box-shadow: 0 0 0 3px rgba(242, 101, 34, 0.1);
            outline: none;
        }

        /* Filtros Section */
        .filters-section {
            padding: 16px 20px;
            border-bottom: 1px solid var(--border-color);
        }

        .section-title {
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: var(--brand-orange);
            margin-bottom: 12px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .section-title i {
            font-size: 12px;
        }

        /* Lista de unidades no estilo da lista de veículos */
        .units-list {
            max-height: 200px;
            overflow-y: auto;
            border: 1px solid var(--border-color);
            border-radius: 16px;
            background: var(--input-bg);
        }

        .unit-row {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 12px;
            cursor: pointer;
            border-bottom: 1px solid var(--border-color);
            transition: all 0.3s ease;
            font-size: 12px;
            color: var(--text-primary);
        }

        .unit-row:last-child {
            border-bottom: none;
        }

        .unit-row:hover {
            background: rgba(242, 101, 34, 0.1);
            transform: translateX(5px);
        }

        .unit-row input[type=checkbox] {
            accent-color: var(--brand-orange);
            width: 16px;
            height: 16px;
            cursor: pointer;
            transition: transform 0.3s ease;
        }

        .unit-row:hover input[type=checkbox] {
            transform: scale(1.1);
        }

        .unit-row.all-row {
            background: var(--sidebar-header-bg);
            font-weight: 700;
            color: var(--brand-orange);
            border-bottom: 2px solid var(--border-color);
            position: sticky;
            top: 0;
            z-index: 1;
        }

        .sel-count {
            font-size: 11px;
            color: var(--brand-silver);
            margin-top: 8px;
            padding: 4px 8px;
            background: var(--input-bg);
            border-radius: 20px;
            display: inline-block;
        }

        /* Pills no estilo do live-tracking */
        .pills {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin-top: 8px;
        }

        .pill {
            padding: 6px 14px;
            border: 1px solid var(--border-color);
            border-radius: 40px;
            font-size: 11px;
            font-weight: 600;
            cursor: pointer;
            color: var(--brand-silver);
            background: var(--input-bg);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            white-space: nowrap;
            position: relative;
            overflow: hidden;
        }

        .pill::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(242, 101, 34, 0.15);
            transform: translate(-50%, -50%);
            transition: width 0.5s, height 0.5s;
        }

        .pill:hover::before {
            width: 150px;
            height: 150px;
        }

        .pill:hover {
            border-color: var(--brand-orange);
            color: var(--brand-orange);
            transform: translateY(-2px);
        }

        .pill.active {
            background: linear-gradient(135deg, var(--brand-orange), #ff8c52);
            color: #fff;
            border-color: var(--brand-orange);
            box-shadow: 0 4px 12px rgba(242, 101, 34, 0.3);
        }

        /* Date picker */
        .date-pair {
            display: none;
            flex-direction: column;
            gap: 12px;
            margin-top: 12px;
        }

        .date-pair.show {
            display: flex;
        }

        .dp-row {
            display: flex;
            flex-direction: column;
            gap: 5px;
        }

        .dp-lbl {
            font-size: 11px;
            color: var(--brand-silver);
            font-weight: 600;
        }

        .dp-row input {
            padding: 8px 12px;
            border: 1px solid var(--border-color);
            border-radius: 12px;
            background: var(--input-bg);
            color: var(--text-primary);
            font-size: 12px;
            transition: all 0.3s ease;
        }

        .dp-row input:focus {
            outline: none;
            border-color: var(--brand-orange);
            box-shadow: 0 0 0 3px rgba(242, 101, 34, 0.1);
        }

        /* Selects */
        .filter-select {
            width: 100%;
            padding: 8px 12px;
            border: 1px solid var(--border-color);
            border-radius: 12px;
            background: var(--input-bg);
            color: var(--text-primary);
            font-size: 12px;
            margin-bottom: 12px;
            transition: all 0.3s ease;
        }

        .filter-select:focus {
            outline: none;
            border-color: var(--brand-orange);
        }

        /* Checkbox row */
        .chk-row {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-top: 12px;
            font-size: 12px;
            color: var(--text-secondary);
            cursor: pointer;
            padding: 6px 0;
        }

        .chk-row input {
            accent-color: var(--brand-orange);
            width: 16px;
            height: 16px;
            cursor: pointer;
        }

        /* Status e progresso */
        .sb-status {
            margin: 12px 20px;
            padding: 10px 14px;
            border-radius: 12px;
            font-size: 12px;
            display: none;
            line-height: 1.4;
        }

        .sb-status.info {
            background: rgba(242, 101, 34, 0.15);
            color: #ff8c52;
            border: 1px solid rgba(242, 101, 34, 0.3);
            display: block;
        }

        .sb-status.error {
            background: rgba(231, 76, 60, 0.15);
            color: #ff6b6b;
            border: 1px solid rgba(231, 76, 60, 0.3);
            display: block;
        }

        .sb-status.loading {
            background: rgba(243, 156, 18, 0.15);
            color: #f1c40f;
            border: 1px solid rgba(243, 156, 18, 0.3);
            display: block;
        }

        .progress-bar {
            margin: 0 20px 12px;
            height: 4px;
            background: var(--border-color);
            border-radius: 4px;
            overflow: hidden;
            display: none;
        }

        .progress-bar.show {
            display: block;
        }

        .pb-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--brand-orange), #ff8c52);
            width: 0%;
            transition: width 0.3s ease;
            animation: pulseProgress 1.5s infinite;
        }

        @keyframes pulseProgress {

            0%,
            100% {
                opacity: 1;
            }

            50% {
                opacity: 0.6;
            }
        }

        /* Botão executar */
        .btn-exec {
            margin: 16px 20px;
            padding: 12px 0;
            background: linear-gradient(135deg, var(--brand-orange), #ff8c52);
            color: #fff;
            border: none;
            border-radius: 40px;
            font-size: 13px;
            font-weight: 700;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .btn-exec::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .btn-exec:hover::before {
            width: 300px;
            height: 300px;
        }

        .btn-exec:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(242, 101, 34, 0.4);
        }

        .btn-exec:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        /* Main area */
        .main-area {
            flex: 1;
            background: var(--card-bg-solid);
            border: 1px solid var(--border-color);
            border-radius: 24px;
            display: flex;
            flex-direction: column;
            overflow: hidden;
            transition: all 0.3s ease;
            margin-right: 30px;
        }

        .main-area:hover {
            border-color: rgba(242, 101, 34, 0.3);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        /* Summary strip */
        .summary-strip {
            background: var(--toolbar-bg);
            border-bottom: 1px solid var(--border-color);
            padding: 12px 20px;
            display: flex;
            align-items: center;
            gap: 24px;
            flex-wrap: wrap;
        }

        .summary-strip.hidden {
            display: none;
        }

        .sg {
            display: flex;
            flex-direction: column;
        }

        .sg .sv {
            font-size: 20px;
            font-weight: 800;
            background: linear-gradient(135deg, var(--brand-orange), #ff8c52);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            line-height: 1.2;
        }

        .sg .sl {
            font-size: 10px;
            color: var(--brand-silver);
            text-transform: uppercase;
            letter-spacing: 0.4px;
        }

        .strip-sep {
            width: 1px;
            height: 32px;
            background: var(--border-color);
        }

        /* Toolbar */
        .result-toolbar {
            background: var(--toolbar-bg);
            border-bottom: 1px solid var(--border-color);
            padding: 10px 20px;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .result-toolbar.hidden {
            display: none;
        }

        .result-toolbar input {
            flex: 1;
            max-width: 280px;
            padding: 8px 14px;
            border: 1px solid var(--border-color);
            border-radius: 40px;
            font-size: 12px;
            background: var(--input-bg);
            color: var(--text-primary);
            transition: all 0.3s ease;
        }

        .result-toolbar input:focus {
            outline: none;
            border-color: var(--brand-orange);
            box-shadow: 0 0 0 3px rgba(242, 101, 34, 0.1);
        }

        .tb-right {
            margin-left: auto;
            display: flex;
            gap: 10px;
            align-items: center;
        }

        .btn-sm {
            padding: 6px 16px;
            border: 1px solid var(--border-color);
            border-radius: 40px;
            background: var(--input-bg);
            font-size: 11px;
            font-weight: 600;
            cursor: pointer;
            color: var(--brand-silver);
            transition: all 0.3s ease;
        }

        .btn-sm:hover {
            border-color: var(--brand-orange);
            color: var(--brand-orange);
            transform: translateY(-1px);
        }

        /* Table */
        .table-wrap {
            flex: 1;
            overflow: auto;
            padding: 0;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 12px;
            min-width: 1000px;
        }

        thead th {
            position: sticky;
            top: 0;
            background: var(--table-header-bg);
            border-bottom: 2px solid var(--border-color);
            padding: 12px 12px;
            text-align: left;
            font-size: 11px;
            font-weight: 700;
            color: var(--brand-silver);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            white-space: nowrap;
            cursor: pointer;
            user-select: none;
            transition: all 0.2s ease;
        }

        thead th:hover {
            color: var(--brand-orange);
        }

        thead th.sorted {
            color: var(--brand-orange);
        }

        thead th.unit-col {
            background: var(--sidebar-header-bg);
            color: var(--brand-orange);
        }

        .sort-icon {
            opacity: 0.4;
            font-size: 9px;
            margin-left: 4px;
        }

        thead th.sorted .sort-icon {
            opacity: 1;
        }

        tbody tr {
            border-bottom: 1px solid var(--border-color);
            transition: all 0.2s ease;
        }

        tbody tr:hover {
            background: rgba(242, 101, 34, 0.05);
        }

        td {
            padding: 10px 12px;
            color: var(--text-primary);
            white-space: nowrap;
            vertical-align: middle;
        }

        td.mono {
            font-family: monospace;
            font-size: 11px;
            color: var(--brand-silver);
        }

        td.unit-cell {
            font-weight: 700;
            color: var(--brand-orange);
            max-width: 200px;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .badge {
            display: inline-block;
            padding: 2px 8px;
            border-radius: 20px;
            font-size: 10px;
            font-weight: 700;
        }

        .bg-green {
            background: rgba(46, 204, 113, 0.2);
            color: #2ecc71;
        }

        .bg-red {
            background: rgba(231, 76, 60, 0.2);
            color: #e74c3c;
        }

        .bg-orange {
            background: rgba(242, 101, 34, 0.2);
            color: var(--brand-orange);
        }

        .param-tag {
            display: inline-block;
            background: rgba(242, 101, 34, 0.15);
            border: 1px solid rgba(242, 101, 34, 0.3);
            border-radius: 6px;
            padding: 2px 8px;
            font-size: 10px;
            font-family: monospace;
            color: #ff8c52;
        }

        .speed-bar {
            display: inline-block;
            height: 4px;
            border-radius: 2px;
            min-width: 2px;
            vertical-align: middle;
            margin-right: 6px;
        }

        /* Empty state */
        .empty-state {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100%;
            color: var(--brand-silver);
            gap: 12px;
            font-size: 13px;
        }

        .empty-state .icon {
            font-size: 48px;
            opacity: 0.3;
        }

        /* Pagination */
        .pag-bar {
            background: var(--toolbar-bg);
            border-top: 1px solid var(--border-color);
            padding: 10px 20px;
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 12px;
            color: var(--brand-silver);
        }

        .pag-bar.hidden {
            display: none;
        }

        .pag-right {
            margin-left: auto;
            display: flex;
            gap: 6px;
        }

        .pg-btn {
            padding: 5px 12px;
            border: 1px solid var(--border-color);
            border-radius: 20px;
            background: var(--input-bg);
            font-size: 11px;
            cursor: pointer;
            color: var(--text-primary);
            transition: all 0.3s ease;
        }

        .pg-btn:hover:not(:disabled) {
            border-color: var(--brand-orange);
            color: var(--brand-orange);
            transform: translateY(-1px);
        }

        .pg-btn:disabled {
            opacity: 0.4;
            cursor: not-allowed;
        }

        .pg-btn.active {
            background: var(--brand-orange);
            color: #fff;
            border-color: var(--brand-orange);
        }

        .spinner {
            width: 16px;
            height: 16px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-top-color: #fff;
            border-radius: 50%;
            animation: spin 0.7s linear infinite;
        }

        @keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }

        /* Toast */
        .toast {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background: var(--brand-orange);
            color: white;
            padding: 12px 24px;
            border-radius: 40px;
            display: none;
            align-items: center;
            gap: 10px;
            z-index: 2100;
            box-shadow: 0 8px 20px rgba(242, 101, 34, 0.4);
            animation: toastSlide 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            font-size: 13px;
            font-weight: 500;
        }

        @keyframes toastSlide {
            from {
                opacity: 0;
                transform: translateX(50px) scale(0.8);
            }

            to {
                opacity: 1;
                transform: translateX(0) scale(1);
            }
        }

        /* Scrollbar */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: var(--sidebar-bg);
        }

        ::-webkit-scrollbar-thumb {
            background: var(--brand-orange);
            border-radius: 3px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #ff8c52;
        }
    </style>
</head>

<body class="theme-dark">
    <top-navigation></top-navigation>

    <div class="list-container">
        <!-- Breadcrumb bar igual ao live-tracking -->
        <div class="breadcrumb-bar">
            <div style="display: flex; gap: 8px;">
                <span class="tab active" onclick="location.href='list.html'">
                    <i class="fas fa-list-alt" style="margin-right: 6px;"></i> Log de Mensagens
                </span>
            </div>

            <div class="org-selector">
                <span><i class="far fa-building"></i> Organização:</span>
                <select id="orgSelector" onchange="changeOrganization(this)">
                    <option value="1">Track7 BR - Betânia</option>
                    <option value="2">Track7 BR - Sul</option>
                    <option value="3">Track7 BR - Norte</option>
                </select>
            </div>
        </div>

        <div class="layout">
            <!-- Filters Panel - mesmo formato do vehicle-panel do live-tracking -->
            <aside class="filters-panel">
                <div class="search-box">
                    <i class="fas fa-search"></i>
                    <input type="text" id="unitSearch" placeholder="Buscar unidade..." oninput="filterUnitList()">
                </div>

                <!-- Área rolável - tudo que deve rolar fica dentro deste div -->
                <div class="filters-content">
                    <div class="filters-section">
                        <div class="section-title">
                            <i class="fas fa-tachometer-alt"></i> Unidades / Equipamentos
                        </div>
                        <div class="units-list" id="unitListWrap">
                            <div class="unit-row all-row" id="rowAll" onclick="toggleAll()">
                                <input type="checkbox" id="chkAll" /> Todos os equipamentos
                            </div>
                        </div>
                        <div class="sel-count" id="selCount">Nenhuma unidade selecionada</div>
                    </div>

                    <div class="filters-section">
                        <div class="section-title">
                            <i class="fas fa-calendar-alt"></i> Período
                        </div>
                        <div class="pills" id="periodPills">
                            <span class="pill" onclick="setQuick(this,'today')">Hoje</span>
                            <span class="pill" onclick="setQuick(this,'yesterday')">Ontem</span>
                            <span class="pill" onclick="setQuick(this,'week')">7 dias</span>
                            <span class="pill active" onclick="setQuick(this,'month')">30 dias</span>
                            <span class="pill" onclick="setQuick(this,'ytd')">Ano atual</span>
                            <span class="pill" onclick="setQuick(this,'custom')">Personalizado</span>
                        </div>
                        <div class="date-pair" id="customDates">
                            <div class="dp-row">
                                <span class="dp-lbl">De</span>
                                <input type="datetime-local" id="dtFrom" />
                            </div>
                            <div class="dp-row">
                                <span class="dp-lbl">Até</span>
                                <input type="datetime-local" id="dtTo" />
                            </div>
                        </div>
                    </div>

                    <div class="filters-section">
                        <div class="section-title">
                            <i class="fas fa-envelope"></i> Tipo de Mensagem
                        </div>
                        <select class="filter-select" id="msgType">
                            <option value="1">Mensagens de dados (GPS)</option>
                            <option value="256">Mensagens customizadas</option>
                            <option value="2">Mensagens SMS</option>
                            <option value="4">Dados inválidos</option>
                        </select>

                        <div class="section-title" style="margin-top: 12px;">
                            <i class="fas fa-layer-group"></i> Limite por Unidade
                        </div>
                        <select class="filter-select" id="maxMsgs">
                            <option value="50" selected>50 mensagens</option>
                            <option value="1000">1.000 mensagens</option>
                            <option value="5000">5.000 mensagens</option>
                            <option value="10000">10.000 mensagens</option>
                            <option value="50000">50.000 mensagens</option>
                            <option value="4294967295">Sem limite</option>
                        </select>

                        <label class="chk-row">
                            <input type="checkbox" id="chkParams" checked> Exibir parâmetros extras
                        </label>
                        <label class="chk-row">
                            <input type="checkbox" id="chkOnlyPos"> Somente com GPS válido
                        </label>
                    </div>

                    <div class="sb-status" id="sbStatus"></div>
                    <div class="progress-bar" id="progressBar">
                        <div class="pb-fill" id="pbFill" style="width:0%"></div>
                    </div>
                </div>

                <!-- Botão executar fica fora da área rolável (fixo na parte inferior) -->
                <button class="btn-exec" id="btnExec" onclick="execute()">
                    <span id="btnContent"><i class="fas fa-play"></i> Executar Consulta</span>
                </button>
            </aside>

            <!-- Main Area -->
            <main class="main-area">
                <!-- Summary Strip -->
                <div class="summary-strip hidden" id="summaryStrip">
                    <div class="sg">
                        <div class="sv" id="sumTotal">0</div>
                        <div class="sl">Mensagens</div>
                    </div>
                    <div class="strip-sep"></div>
                    <div class="sg">
                        <div class="sv" id="sumUnits">0</div>
                        <div class="sl">Unidades</div>
                    </div>
                    <div class="strip-sep"></div>
                    <div class="sg">
                        <div class="sv" id="sumPeriod">--</div>
                        <div class="sl">Período</div>
                    </div>
                    <div class="strip-sep"></div>
                    <div class="sg">
                        <div class="sv" id="sumMaxSpd">--</div>
                        <div class="sl">Vel. Máx.</div>
                    </div>
                    <div class="strip-sep"></div>
                    <div class="sg">
                        <div class="sv" id="sumAvgSpd">--</div>
                        <div class="sl">Vel. Média</div>
                    </div>
                    <div class="strip-sep"></div>
                    <div class="sg">
                        <div class="sv" id="sumWithPos">--</div>
                        <div class="sl">Com GPS</div>
                    </div>
                    <div class="strip-sep"></div>
                    <div class="sg">
                        <div class="sv" id="sumParamKeys">--</div>
                        <div class="sl">Parâmetros</div>
                    </div>
                </div>

                <!-- Toolbar -->
                <div class="result-toolbar hidden" id="resultToolbar">
                    <input type="text" id="searchInput" placeholder="Filtrar em qualquer coluna..."
                        oninput="applySearch()" />
                    <div class="tb-right">
                        <button class="btn-sm" onclick="exportCSV()"><i class="fas fa-file-csv"></i> CSV</button>
                        <button class="btn-sm" onclick="exportJSON()"><i class="fas fa-file-code"></i> JSON</button>
                        <span style="font-size: 11px; color: var(--brand-silver);" id="pageInfo"></span>
                    </div>
                </div>

                <!-- Table -->
                <div class="table-wrap">
                    <div class="empty-state" id="emptyState">
                        <div class="icon"><i class="fas fa-database"></i></div>
                        <div>Selecione equipamentos e clique em <strong>Executar</strong></div>
                        <div style="font-size: 11px; color: var(--brand-silver);">Todos os campos disponíveis do log
                            serão exibidos</div>
                    </div>
                    <table id="dataTable" style="display: none;">
                        <thead id="tHead"></thead>
                        <tbody id="tBody"></tbody>
                    </table>
                </div>

                <!-- Pagination -->
                <div class="pag-bar hidden" id="pagBar">
                    <span id="pagInfo"></span>
                    <div class="pag-right" id="pagBtns"></div>
                </div>
            </main>
        </div>
    </div>

    <div class="toast" id="toast">
        <i class="fas fa-check-circle"></i>
        <span id="toastMessage">Operação realizada!</span>
    </div>

    <script>
        const API = 'http://localhost:5000/api';
        const PAGE_SIZE = 200;

        let allUnits = [];
        let allRows = [];
        let filtered = [];
        let colHeaders = [];
        let allParamKeys = new Set();
        let currentPage = 1;
        let sortCol = null, sortAsc = true;
        let timeFrom = 0, timeTo = 0;

        function showToast(msg, type = 'success') {
            const toast = document.getElementById('toast');
            const icon = toast.querySelector('i');
            const msgSpan = document.getElementById('toastMessage');
            if (type === 'success') {
                icon.className = 'fas fa-check-circle';
                toast.style.background = 'var(--brand-orange)';
            } else {
                icon.className = 'fas fa-exclamation-circle';
                toast.style.background = '#e74c3c';
            }
            msgSpan.textContent = msg;
            toast.style.display = 'flex';
            setTimeout(() => { toast.style.display = 'none'; }, 3000);
        }

        function changeOrganization(select) {
            showToast(`Organização alterada: ${select.options[select.selectedIndex].text}`);
        }

        document.addEventListener('DOMContentLoaded', () => {
            const savedTheme = localStorage.getItem('theme') || 'dark';
            document.body.className = `theme-${savedTheme}`;
            applyPeriod('month');
            checkServer();
        });

        document.addEventListener('themeChanged', (e) => {
            document.body.className = `theme-${e.detail.theme}`;
        });

        function renderUnitList(units) {
            const wrap = document.getElementById('unitListWrap');
            [...wrap.querySelectorAll('.unit-item')].forEach(el => el.remove());
            units.forEach(u => {
                const row = document.createElement('label');
                row.className = 'unit-row unit-item';
                row.dataset.id = u.id;
                row.dataset.name = u.name;
                row.innerHTML = `<input type="checkbox" class="unit-chk" value="${u.id}" onchange="updateSelCount()"> <span>${escapeHtml(u.name)}</span>`;
                wrap.appendChild(row);
            });
            updateSelCount();
        }

        function filterUnitList() {
            const q = document.getElementById('unitSearch').value.toLowerCase();
            document.querySelectorAll('.unit-item').forEach(row => {
                row.style.display = row.dataset.name.toLowerCase().includes(q) ? '' : 'none';
            });
        }

        function toggleAll() {
            const chkAll = document.getElementById('chkAll');
            chkAll.checked = !chkAll.checked;
            const checked = chkAll.checked;
            document.querySelectorAll('.unit-chk').forEach(c => {
                if (c.closest('.unit-item').style.display !== 'none') c.checked = checked;
            });
            updateSelCount();
        }

        function updateSelCount() {
            const all = document.querySelectorAll('.unit-chk');
            const sel = [...all].filter(c => c.checked);
            const chkAll = document.getElementById('chkAll');
            chkAll.checked = sel.length > 0 && sel.length === all.length;
            document.getElementById('selCount').textContent =
                sel.length === 0 ? 'Nenhuma unidade selecionada' :
                    sel.length === all.length ? `Todos os ${all.length} equipamentos selecionados` :
                        `${sel.length} de ${all.length} selecionado(s)`;
        }

        function getSelectedUnits() {
            return [...document.querySelectorAll('.unit-chk:checked')].map(c => {
                const row = c.closest('.unit-item');
                return { id: c.value, name: row.dataset.name };
            });
        }

        async function checkServer() {
            try {
                const r = await fetch(`${API}/status`);
                const d = await r.json();
                setStatus('info', 'Servidor conectado');
                if (d.authenticated) {
                    await loadUnits();
                    setStatus('info', 'Sessão ativa. Selecione equipamentos e execute.');
                } else {
                    await doLogin();
                }
            } catch (e) {
                setStatus('error', 'Servidor offline. Execute: python server.py na pasta wialon_api/');
            }
        }

        async function doLogin() {
            setStatus('loading', 'Autenticando no Wialon...');
            try {
                const r = await fetch(`${API}/login`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ token: '67eba42da80819eb285dd8794678964707481B6FFB3E065405FE46F889143875DE5B3C3A' })
                });
                const d = await r.json();
                if (d.error) throw new Error(d.error);
                setStatus('info', 'Login OK. Carregando unidades...');
                await loadUnits();
            } catch (e) {
                setStatus('error', 'Login falhou: ' + e.message);
            }
        }

        async function loadUnits() {
            const r = await fetch(`${API}/units`);
            const d = await r.json();
            if (d.error) { setStatus('error', d.error); return; }
            allUnits = d.units;
            renderUnitList(allUnits);
            setStatus('info', d.units.length + ' unidade(s). Selecione e execute.');
        }

        async function execute() {
            const units = getSelectedUnits();
            if (!units.length) { setStatus('error', 'Selecione pelo menos uma unidade.'); return; }

            const msgType = parseInt(document.getElementById('msgType').value);
            const maxCount = parseInt(document.getElementById('maxMsgs').value);
            const onlyPos = document.getElementById('chkOnlyPos').checked;
            const showParams = document.getElementById('chkParams').checked;

            setBusy(true);
            showProgress();

            let allMsgs = [];
            let allParamSet = new Set();

            for (let i = 0; i < units.length; i++) {
                const u = units[i];
                const pct = Math.round(((i + 1) / units.length) * 100);
                document.getElementById('pbFill').style.width = pct + '%';
                setStatus('loading', `Carregando ${i + 1}/${units.length}: ${u.name}...`);

                try {
                    const r = await fetch(`${API}/messages`, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ unitId: u.id, timeFrom, timeTo, msgType, maxCount })
                    });
                    const d = await r.json();
                    if (d.error) throw new Error(d.error);

                    const msgs = d.messages || [];
                    msgs.forEach(m => {
                        m._unitName = u.name;
                        m._unitId = u.id;
                        Object.keys(m.p || {}).forEach(k => allParamSet.add(k));
                    });
                    allMsgs = allMsgs.concat(msgs);
                } catch (e) {
                    setStatus('error', `Erro em "${u.name}": ${e.message}`);
                }
            }

            buildTable(allMsgs, allParamSet, onlyPos, showParams);
            setStatus('info', allMsgs.length.toLocaleString('pt-BR') + ' mensagens de ' + units.length + ' unidade(s).');
            setBusy(false);
            hideProgress();
        }

        function buildTable(msgs, paramSetArg, onlyPos, showParams) {
            allParamKeys = paramSetArg || new Set();
            const paramKeys = [...allParamKeys].sort();

            const cols = [
                { key: '_idx', label: '#', mono: true, unitCol: false },
                { key: 'unit', label: 'Unidade', mono: false, unitCol: true },
                { key: 'dt', label: 'Data / Hora', mono: true, unitCol: false },
                { key: 'lat', label: 'Latitude', mono: true, unitCol: false },
                { key: 'lon', label: 'Longitude', mono: true, unitCol: false },
                { key: 'spd', label: 'Vel. (km/h)', speed: true, unitCol: false },
                { key: 'course', label: 'Curso (°)', mono: false, unitCol: false },
                { key: 'alt', label: 'Alt. (m)', mono: false, unitCol: false },
                { key: 'sat', label: 'Sats', sat: true, unitCol: false },
                { key: 'flags', label: 'Flags', mono: true, unitCol: false },
            ];
            if (showParams) paramKeys.forEach(k => cols.push({ key: 'p_' + k, label: k, param: true }));
            colHeaders = cols;

            let rows = [];
            msgs.forEach((m, idx) => {
                const pos = m.pos || {};
                if (onlyPos && !pos.y) return;
                const row = {
                    _idx: idx + 1,
                    unit: m._unitName || '',
                    t: m.t || 0,
                    dt: tsToStr(m.t),
                    lat: pos.y != null ? pos.y.toFixed(6) : '',
                    lon: pos.x != null ? pos.x.toFixed(6) : '',
                    spd: pos.s != null ? pos.s : '',
                    course: pos.c != null ? pos.c : '',
                    alt: pos.z != null ? pos.z : '',
                    sat: pos.sc != null ? pos.sc : '',
                    flags: m.f != null ? m.f : '',
                };
                if (showParams) {
                    const p = m.p || {};
                    paramKeys.forEach(k => {
                        const v = p[k];
                        row['p_' + k] = (v != null && typeof v === 'object') ? (v.v != null ? v.v : (v.x != null ? v.x : JSON.stringify(v))) : (v != null ? v : '');
                    });
                }
                rows.push(row);
            });

            const speeds = rows.map(r => parseFloat(r.spd)).filter(v => !isNaN(v) && v >= 0);
            const unitSet = new Set(rows.map(r => r.unit));
            document.getElementById('sumTotal').textContent = rows.length.toLocaleString('pt-BR');
            document.getElementById('sumUnits').textContent = unitSet.size;
            document.getElementById('sumMaxSpd').textContent = (speeds.length ? Math.max(...speeds) : 0) + ' km/h';
            document.getElementById('sumAvgSpd').textContent = (speeds.length ? (speeds.reduce((a, b) => a + b, 0) / speeds.length).toFixed(1) : 0) + ' km/h';
            document.getElementById('sumWithPos').textContent = rows.filter(r => r.lat).length.toLocaleString('pt-BR');
            document.getElementById('sumParamKeys').textContent = allParamKeys.size;
            if (rows.length >= 2) {
                const first = rows[0].dt.slice(0, 10), last = rows[rows.length - 1].dt.slice(0, 10);
                document.getElementById('sumPeriod').textContent = first + ' → ' + last;
            }

            allRows = rows;
            filtered = [...rows];
            currentPage = 1;
            renderTable();
        }

        function renderTable() {
            const empty = document.getElementById('emptyState');
            const table = document.getElementById('dataTable');
            const strip = document.getElementById('summaryStrip');
            const toolbar = document.getElementById('resultToolbar');
            const pagBar = document.getElementById('pagBar');

            if (!filtered.length) {
                table.style.display = 'none'; empty.style.display = 'flex';
                strip.classList.add('hidden'); toolbar.classList.add('hidden'); pagBar.classList.add('hidden');
                return;
            }

            empty.style.display = 'none'; table.style.display = '';
            strip.classList.remove('hidden'); toolbar.classList.remove('hidden'); pagBar.classList.remove('hidden');

            document.getElementById('tHead').innerHTML = '<tr>' + colHeaders.map(c =>
                `<th onclick="sortBy('${c.key}')" class="${c.unitCol ? 'unit-col' : ''} ${sortCol === c.key ? 'sorted' : ''}">
                    ${escapeHtml(c.label)}<span class="sort-icon">${sortCol === c.key ? (sortAsc ? '▲' : '▼') : '⇅'}</span>
                </th>`
            ).join('') + '</tr>';

            const start = (currentPage - 1) * PAGE_SIZE;
            const page = filtered.slice(start, start + PAGE_SIZE);

            document.getElementById('tBody').innerHTML = page.map(row => {
                const spd = parseFloat(row.spd);
                const hasSpd = !isNaN(spd);
                const spdColor = spd > 90 ? '#e74c3c' : spd > 60 ? '#f39c12' : '#27ae60';
                const spdCls = spd > 90 ? 'bg-red' : spd > 60 ? 'bg-orange' : 'bg-green';
                const spdW = hasSpd ? Math.min(spd / 120 * 36, 36) : 0;

                const cells = colHeaders.map(c => {
                    const v = row[c.key];
                    const str = v != null && v !== '' ? String(v) : '';
                    if (c.key === '_idx') return `<td style="color: #666;">${v}</td>`;
                    if (c.unitCol) return `<td class="unit-cell" title="${escapeHtml(str)}">${escapeHtml(str)}</td>`;
                    if (c.speed && hasSpd) return `<td><span class="speed-bar" style="width:${spdW}px;background:${spdColor};"></span><span class="badge ${spdCls}">${spd} km/h</span></td>`;
                    if (c.sat && str) return `<td><span class="badge ${parseInt(str) >= 4 ? 'bg-green' : 'bg-red'}">${str}</span></td>`;
                    if (c.key === 'flags' && str) return `<td class="mono">0x${(parseInt(str) || 0).toString(16).toUpperCase()}</td>`;
                    if (c.param) return str ? `<td><span class="param-tag">${escapeHtml(str)}</span></td>` : `<td style="color: #444;">-</td>`;
                    if (c.mono) return `<td class="mono">${str || '<span style="color:#444;">-</span>'}</td>`;
                    return `<td>${str || '<span style="color:#444;">-</span>'}</td>`;
                });
                return `<tr>${cells.join('')}</tr>`;
            }).join('');

            renderPagination();
        }

        function renderPagination() {
            const total = filtered.length;
            const totalPages = Math.ceil(total / PAGE_SIZE);
            const start = (currentPage - 1) * PAGE_SIZE + 1;
            const end = Math.min(currentPage * PAGE_SIZE, total);

            document.getElementById('pagInfo').textContent = `Exibindo ${start.toLocaleString('pt-BR')}-${end.toLocaleString('pt-BR')} de ${total.toLocaleString('pt-BR')}`;
            document.getElementById('pageInfo').textContent = `Pág. ${currentPage}/${totalPages}`;

            const btns = document.getElementById('pagBtns');
            btns.innerHTML = '';
            const add = (label, pg, active) => {
                const b = document.createElement('button');
                b.className = 'pg-btn' + (active ? ' active' : '');
                b.innerHTML = label;
                b.disabled = pg < 1 || pg > totalPages || active;
                b.onclick = () => { currentPage = pg; renderTable(); };
                btns.appendChild(b);
            };
            add('‹ Anterior', currentPage - 1, false);
            const pages = new Set([1, totalPages, currentPage, currentPage - 1, currentPage + 1].filter(p => p >= 1 && p <= totalPages));
            let last = 0;
            [...pages].sort((a, b) => a - b).forEach(p => {
                if (last && p - last > 1) { const sp = document.createElement('span'); sp.textContent = '...'; sp.style.cssText = 'padding:4px;color:#666;'; btns.appendChild(sp); }
                add(p, p, p === currentPage); last = p;
            });
            add('Próximo ›', currentPage + 1, false);
        }

        function applySearch() {
            const q = document.getElementById('searchInput').value.toLowerCase();
            filtered = !q ? [...allRows] : allRows.filter(row =>
                colHeaders.some(c => { const v = row[c.key]; return v != null && String(v).toLowerCase().includes(q); })
            );
            currentPage = 1;
            renderTable();
        }

        function sortBy(col) {
            sortAsc = sortCol === col ? !sortAsc : true;
            sortCol = col;
            filtered.sort((a, b) => {
                let va = a[col], vb = b[col];
                if (!isNaN(parseFloat(va)) && !isNaN(parseFloat(vb))) { va = parseFloat(va); vb = parseFloat(vb); }
                if (va < vb) return sortAsc ? -1 : 1;
                if (va > vb) return sortAsc ? 1 : -1;
                return 0;
            });
            currentPage = 1;
            renderTable();
        }

        function setQuick(el, period) {
            document.querySelectorAll('#periodPills .pill').forEach(p => p.classList.remove('active'));
            el.classList.add('active');
            const cd = document.getElementById('customDates');
            if (period === 'custom') { cd.classList.add('show'); return; }
            cd.classList.remove('show');
            applyPeriod(period);
        }

        function applyPeriod(period) {
            const now = Math.floor(Date.now() / 1000);
            const d = new Date(); d.setHours(0, 0, 0, 0);
            const tod = Math.floor(d.getTime() / 1000);
            switch (period) {
                case 'today': timeFrom = tod; timeTo = now; break;
                case 'yesterday': timeFrom = tod - 86400; timeTo = tod - 1; break;
                case 'week': timeFrom = now - 7 * 86400; timeTo = now; break;
                case 'month': timeFrom = now - 30 * 86400; timeTo = now; break;
                case 'ytd': timeFrom = Math.floor(new Date(d.getFullYear(), 0, 1).getTime() / 1000); timeTo = now; break;
            }
            const fmt = ts => new Date(ts * 1000).toISOString().slice(0, 16);
            const fi = document.getElementById('dtFrom'), ti = document.getElementById('dtTo');
            if (fi) fi.value = fmt(timeFrom);
            if (ti) ti.value = fmt(timeTo);
        }

        function exportCSV() {
            if (!filtered.length) { showToast('Sem dados para exportar.', 'error'); return; }
            const hdr = colHeaders.map(c => '"' + c.label + '"').join(',');
            const rows = filtered.map(r => colHeaders.map(c => { const v = r[c.key]; return v != null && v !== '' ? '"' + String(v).replace(/"/g, '""') + '"' : '""'; }).join(','));
            download('wialon_log.csv', 'data:text/csv;charset=utf-8,' + encodeURIComponent([hdr, ...rows].join('\n')));
            showToast(`CSV exportado — ${filtered.length.toLocaleString('pt-BR')} linhas.`);
        }

        function exportJSON() {
            if (!filtered.length) { showToast('Sem dados para exportar.', 'error'); return; }
            const data = filtered.map(r => { const o = {}; colHeaders.forEach(c => { if (c.key !== '_idx') o[c.key] = r[c.key]; }); return o; });
            download('wialon_log.json', 'data:application/json;charset=utf-8,' + encodeURIComponent(JSON.stringify(data, null, 2)));
            showToast(`JSON exportado — ${filtered.length.toLocaleString('pt-BR')} registros.`);
        }

        function download(name, href) { const a = document.createElement('a'); a.href = href; a.download = name; a.click(); }

        function tsToStr(ts) { if (!ts) return ''; const d = new Date(ts * 1000); return d.toLocaleString('pt-BR'); }
        function escapeHtml(s) { return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;'); }
        function setStatus(type, msg) { const el = document.getElementById('sbStatus'); el.className = 'sb-status ' + type; el.textContent = msg; }
        function setBusy(b) { const btn = document.getElementById('btnExec'); btn.disabled = b; document.getElementById('btnContent').innerHTML = b ? '<div class="spinner"></div> Carregando...' : '<i class="fas fa-play"></i> Executar Consulta'; }
        function showProgress() { document.getElementById('progressBar').classList.add('show'); }
        function hideProgress() { document.getElementById('progressBar').classList.remove('show'); }

        document.addEventListener('DOMContentLoaded', () => {
            ['dtFrom', 'dtTo'].forEach(id => {
                document.getElementById(id).addEventListener('change', () => {
                    const fv = document.getElementById('dtFrom').value, tv = document.getElementById('dtTo').value;
                    if (fv) timeFrom = Math.floor(new Date(fv).getTime() / 1000);
                    if (tv) timeTo = Math.floor(new Date(tv).getTime() / 1000);
                });
            });
        });
    </script>
</body>

</html>
